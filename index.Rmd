---
layout: default
---

{% for post in site.posts %}
<h3><a href="{{ site.baseurl }}{{ post.url }}">{{ post.title }}{% if post.subtitle %}: {{ post.subtitle }}{% endif %}</a></h3>
<p><small>Posed on {{ post.date | date: '%B %d, %Y' }}</small></p>
<p>{{ post.excerpt }}</p>
{% endfor %}
